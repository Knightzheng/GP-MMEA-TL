# Transfer Case Analysis Thesis Sync (2026-03-15)

## 1. 当前判断

不需要为了论文线程 `6.11` 再重跑正式实验。当前仓库中的正式案例文件已经从早期的 `6` 个样本扩展到 `8` 个样本，且新增的两例正好覆盖了论文线程希望补强的两类材料：

1. `1` 个额外的 `ja_en` 失败 / 边界失败案例；
2. `1` 个额外的跨图谱成功纠错案例。

当前正式入口：

- `reports/transfer/transfer_case_analysis_examples.md`
- `reports/transfer/transfer_case_analysis_examples.csv`

## 2. 与论文线程 6.11 对齐的两条新增样本

| 论文线程需求 | 当前样本 | 数据集 | 索引 | 基线 rank | 方法 rank | 作用 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| 额外 `ja_en` 失败 / 边界失败案例 | `Fat Mike` | `ja_en` | 9563 | 2 | 13 | 继续保留跨语言细粒度近邻混淆，防止把方法写得过满 |
| 额外跨图谱成功纠错案例 | `JavaScript` | `FBDB15K` | 1959 | 274 | 0 | 说明方法能把严重误排样本恢复到 top-1，强化跨图谱 rank recovery 证据 |

## 3. 为什么这轮不再继续扩案例

1. 当前 `8` 个样本已经覆盖“跨语言失败边界 + 跨图谱成功纠错”两类最关键的定性证据。
2. 继续扩样本的边际收益已经明显下降，更适合把精力放在材料同步与可追溯整理上。
3. 若正文篇幅有限，当前更合理的做法不是继续扩充，而是从 `8` 个样本中精选 `6` 个入文，并把剩余 `2` 个放入附录或答辩材料。

## 4. 第四章可直接吸收的分析文字

在现有正式结果基础上，案例分析已扩展到 `8` 个代表性样本，覆盖 `ja_en` 的失败 / 边界失败案例以及 `FBDB15K`、`FBYG15K` 的成功纠错案例。新增样本进一步表明，本文方法的增益并非只体现在均值层面的提升上，在跨图谱场景中还能够将基线模型严重误排的目标实体恢复到 `top-1`；与此同时，`ja_en` 中仍保留了细粒度近邻混淆导致的失败样本，说明当前方法虽然能够提升迁移后的候选质量与整体鲁棒性，但对跨语言细粒度歧义的处理仍存在边界。

## 5. 当前不宜夸大的部分

1. 这批案例只能作为定性证据，不应写成大样本统计规律。
2. `ja_en` 的失败样本说明方法并未覆盖所有困难实体。
3. 跨图谱成功纠错案例支持“rank recovery”这一现象，但不等于已经证明某个局部模块在所有场景下都稳定有效。
