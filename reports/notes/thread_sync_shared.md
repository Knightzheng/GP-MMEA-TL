# 线程共享交接板（论文线程 <-> 项目优化线程）

- 项目：`GP-MMEA-TL`
- 建立日期：`2026-03-14`
- 用途：用于“论文初稿写作线程（线程B）”与“项目优化/证据补强线程（线程A）”之间的稳定信息交接
- 维护原则：只增量追加，不覆盖历史交接；每轮更新都写清楚日期、已完成项、未完成项、文件路径和建议回填位置

## 1. 使用规则

1. 线程B负责写入论文当前进展、已吸收证据、下一轮明确需求、以及希望线程A自主评估的方向。
2. 线程A负责写入本轮新增实验/分析材料、其可支持的论文主张强度、可直接入文的文件路径与数据文件目录。
3. 若线程B本轮没有具体实验需求，也必须明确授权线程A对项目文件进行自主评估，选择当前最高价值的“论文可引用增量材料”继续补强。
4. 线程A回填时，优先提供可直接被论文使用的产物，包括：`md` 小节草稿、`csv` 表格源数据、可复现实验脚本入口、图表建议和结论口径。
5. 所有结论默认遵守当前论文边界：主线聚焦“统一迁移实验链路 + 4 个目标域 5-seed 稳定正增益 + 目标域自适应/伪标签质量控制对迁移性能与稳定性具有积极作用”，不夸大为“所有辅助模块都已被充分严格证明单独有效”。

## 2. 当前稳定主线（供两个线程统一口径）

截至 `2026-03-14`，论文与项目当前最稳的主结论为：

1. 已建立统一的 `source-train -> target-adapt` 迁移实验链路。
2. 正式主表固定为 4 个目标域：`ja_en`、`fr_en`、`FBDB15K`、`FBYG15K`。
3. 四个目标域均已形成 `5-seed` 正增益正式结果。
4. 当前更有支撑力的机制结论来自“目标域自适应 + 伪标签质量控制”，而不是对 `source_select`、`missing_gate` 等辅助模块单独效果的强证明。

## 3. 线程B -> 线程A：当前进展与本轮需求（2026-03-14）

### 3.1 论文线程当前进展

论文初稿主文件：

- `D:\code\codes\cursor\BYSJ_zyf\reports\thesis\本科毕业论文初稿_v1.md`

当前已经写实并完成第二轮吸收的部分：

1. 中文摘要已同步到第二轮证据版本。
2. 第一章、第二章、第三章主体已成型，并且方法主张已收束到“目标域自适应 + 伪标签质量控制”。
3. 第四章已经吸收以下内容：
   - 4 个目标域正式主表；
   - `5-seed mean±std`；
   - 统计显著性与稳定性分析；
   - `zh_en` 多 seed 消融；
   - 误差分桶与阶段机理分析；
   - 6 个案例分析；
   - wall-clock 效率分析；
   - 局限性分析。
4. 第五章“结论与展望”已从占位状态推进为正式章节。

当前论文中已经吸收、因此线程A无需重复投入主精力的材料：

1. `transfer_adapt_significance_summary.md`
2. `transfer_adapt_significance_writeup.md`
3. `transfer_case_analysis_examples.md`
4. `transfer_efficiency_summary.md`

### 3.2 线程B当前对线程A的明确需求

请线程A优先围绕“最能增强论文终稿与答辩稳健性”的方向继续推进，优先级如下。

#### P1：高优先级

1. 若成本可控，请优先补最小版 `H3` 缺失模态压力测试。
   - 目标：哪怕只做最小可用版本，也要形成能直接回填第四章和第五章的结论。
   - 输出优先形式：`md` 结果总结 + `csv` 主表 + 可复现实验脚本入口。
2. 请补最小版 GPU 峰值显存测量。
   - 建议最小范围：`baseline/method × 2 个代表性目标域 × seed=42`。
   - 输出优先形式：`md` 小节草稿 + `csv` 表格源数据。

#### P2：中优先级

3. 若代价较低，请把案例从当前 `6` 个扩到 `8` 个。
   - 优先新增：`1` 个 `ja_en` 失败/边界案例，`1` 个跨图谱成功纠错案例。
4. 若方便，请补“图表友好型”整理结果。
   - 例如：显著性置信区间绘图数据、案例图所需排序摘要、效率图所需归一化表。

#### P3：条件允许时再做

5. 若时间和算力允许，再评估补 `1` 个额外 baseline。
   - 若不可行，请直接产出一段论文可用的“局限性表述”和适用范围说明。

### 3.3 若本轮没有明确可做项，请线程A自主评估并继续优化

如果以上请求因时间、算力或环境约束不适合执行，请线程A不要空转，也不要等待指令。请直接：

1. 自主评估项目当前文件与已有运行产物；
2. 选择“对论文最有价值、且最容易快速形成入文证据”的方向继续补强；
3. 优先输出论文可直接引用的内容，而不是只留下实验草稿；
4. 在本共享文件中写清楚：
   - 做了什么；
   - 新增材料能支撑什么；
   - 不能支撑什么；
   - 对应文件与数据文件目录；
   - 建议论文回填位置。

### 3.4 线程B希望线程A回填时遵循的输出格式

请线程A在后续回填时尽量使用下面的结构：

1. 本轮实际完成内容总览
2. 本轮未完成内容
3. 新增或更新的文件目录与路径
4. 面向论文的回填建议
5. 当前仍不宜夸大的部分
6. 可直接粘贴给论文线程的总结段落

### 3.5 线程B本轮新增需求（2026-03-14 晚）

论文线程本轮已经继续向“可提交导师审阅的完整初稿”推进，新增完成了以下内容：

1. 英文摘要已补齐并与中文摘要、主结论保持一致。
2. 论文插图已生成并插入正文，包括技术路线图、方法框架图、显著性可视化图、场景增益图、阶段路径图和案例排序图。
3. 参考文献初版已统一补入正文末尾。
4. 致谢草稿与附录骨架已补入，用于形成更完整的论文文档结构。
5. 线程B已经读取并处理了你在 `4.8` 中关于 `H3/GPU` 的基础能力接入说明，但**尚未**将其写成正文结果，因为目前还没有正式数据。

因此，线程A后续最值得优先产出的内容已经进一步收敛为：

#### P1：把“基础能力已接入”推进为“最小版正式结果已产出”

1. 请优先尝试跑出最小版 `H3` 缺失模态压力测试正式结果。
   - 输出最低要求：
     - `md` 结果总结
     - `csv` 主表
     - 明确实验矩阵、目标域、seed、drop_rate 设置
     - 一段可直接进论文第四章或第五章的结论句
     - 一段明确“当前能支持什么、不能支持什么”的边界说明
2. 请优先尝试跑出最小版 GPU 峰值显存正式补测结果。
   - 输出最低要求：
     - `md` 小节草稿
     - `csv` 汇总表
     - baseline/method、目标域、seed 的具体范围说明
     - 一段可直接进论文效率分析小节的表述

#### P2：如果正式结果仍暂时跑不出来，也请不要空转

3. 若 `H3` 或 GPU 补测本轮仍受阻，请直接补一份“阻塞说明 + 论文可用局限性表述”。
   - 需要明确：
     - 当前卡在哪里
     - 预计最小可交付版本还差什么
     - 论文里应如何保守书写
4. 若成本较低，请继续把案例从 `6` 个扩到 `8` 个，并输出更适合附录或图表整理的结构化数据。

#### P3：论文线程当前不再需要你重复投入的内容

5. 统计显著性、6 个案例、wall-clock 效率、图像型论文插图、英文摘要、参考文献初版、致谢草稿和附录骨架，论文线程已经吸收或自行补齐。
6. 因此你后续最有价值的投入，不是重复整理已有材料，而是把 `H3/GPU` 补成真正能入文的正式结果，或在做不到时给出更强的论文边界表述。

## 4. 线程A -> 线程B：本轮/下轮回填区（由项目优化线程追加）

### 4.1 更新时间

- `2026-03-14`
- 本次回填性质：第二轮交接整理，基于本轮已完成的显著性补强、案例分析、wall-clock 效率整理进行规范交接。

### 4.2 本轮实际完成内容总览

- 已完成：
  1. 基于当前统一 `4` 个目标域 `5-seed` 正式结果，补齐了统计显著性与稳定性分析。
  2. 基于现有保留的 `pred.txt`，补齐了 `6` 个案例级样本，覆盖 `ja_en` 失败/边界案例，以及 `FBDB15K/FBYG15K` 成功纠错案例。
  3. 基于 formal `log.txt`，补齐了 wall-clock 效率汇总，形成了可直接入文的效率表草稿。
  4. 新增了三类可复现脚本入口，分别对应显著性、案例分析和效率统计。
  5. 已将上述材料入口同步写入 `README.md`、`PROJECT_OPERATION_RECORD.md`、`PROCESS_LOG.md`。
- 本轮没有新增的内容：
  1. 没有新增主实验结果，也没有切换当前 `4` 目标主表版本。
  2. 没有完成 `H3` 缺失模态压力测试。
  3. 没有补齐 GPU 峰值显存测量。
  4. 没有补 1 个额外 baseline。

### 4.3 本轮未完成内容

- `H3` 最小版缺失模态压力测试：未完成。
  - 原因：本轮优先处理了更低成本、可直接支撑论文和答辩的统计/案例/效率材料。
- GPU 峰值显存测量：未完成。
  - 原因：现有 formal run 未统一记录显存，需要最小补测。
- 案例从 `6` 个扩到 `8` 个：未完成。
  - 原因：当前 `6` 个样本已足够形成入文小节，但后续仍可扩展。
- 额外 baseline：未完成。
  - 原因：当前公平迁移链路主要建立在 `MEAformer-based transfer setting` 上，补 1 个新 baseline 的成本仍较高。

### 4.4 新增或更新的文件目录与路径

- 统计显著性：
  - `reports/transfer/transfer_adapt_significance_summary.md`
    - 用途：显著性主表与统计口径说明
    - 论文对应：第四章“统计显著性与稳定性分析”
    - 优先级：高
    - 可直接入文：是
  - `reports/transfer/transfer_adapt_significance_summary.csv`
    - 用途：显著性主表原始数据
    - 论文对应：第四章结果表制作
    - 优先级：高
    - 可直接入文：部分可用
  - `reports/transfer/transfer_adapt_significance_per_seed.csv`
    - 用途：每个 seed 的配对统计明细
    - 论文对应：附录/答辩追问
    - 优先级：中
    - 可直接入文：部分可用
  - `reports/transfer/transfer_adapt_significance_writeup.md`
    - 用途：论文正文段落模板与答辩口径
    - 论文对应：第四章正文、答辩问答
    - 优先级：高
    - 可直接入文：是
- 案例分析：
  - `reports/transfer/transfer_case_analysis_examples.md`
    - 用途：案例分析小节草稿、案例表、逐例结论
    - 论文对应：第四章“案例分析”
    - 优先级：高
    - 可直接入文：是
  - `reports/transfer/transfer_case_analysis_examples.csv`
    - 用途：案例结构化数据
    - 论文对应：第四章表格制作、附录
    - 优先级：高
    - 可直接入文：部分可用
- 效率分析：
  - `reports/transfer/transfer_efficiency_summary.md`
    - 用途：效率分析小节草稿与 wall-clock 汇总表
    - 论文对应：第四章“效率分析”
    - 优先级：中
    - 可直接入文：是
  - `reports/transfer/transfer_efficiency_summary.csv`
    - 用途：效率表原始数据
    - 论文对应：第四章表格制作
    - 优先级：中
    - 可直接入文：部分可用
  - `reports/transfer/transfer_efficiency_per_run.csv`
    - 用途：每个 run 的耗时明细
    - 论文对应：附录/答辩追问
    - 优先级：低
    - 可直接入文：暂不建议
- 可复现脚本：
  - `scripts/analyze_transfer_significance.py`
  - `scripts/build_transfer_case_analysis.py`
  - `scripts/summarize_transfer_efficiency.py`
    - 用途：复现本轮三类支撑材料
    - 论文对应：可复现性说明、答辩备查
    - 优先级：中
    - 可直接入文：暂不建议
- 记录同步：
  - `README.md`
  - `PROJECT_OPERATION_RECORD.md`
  - `PROCESS_LOG.md`
    - 用途：补充本轮材料入口与阶段记录
    - 论文对应：材料导航、过程备查
    - 优先级：中
    - 可直接入文：部分可用

### 4.5 面向论文的回填建议

- 第四章主结果后：
  - 优先插入显著性与稳定性分析。
  - 口径建议：强调“配对 seed 分析”“`5/5` 正增益”“bootstrap `95% CI` 下界大于 `0`”。
  - 对应文件：
    - `reports/transfer/transfer_adapt_significance_summary.md`
    - `reports/transfer/transfer_adapt_significance_writeup.md`
- 第四章案例分析小节：
  - 直接吸收当前 `6` 个案例样本。
  - 口径建议：
    - `FBDB15K/FBYG15K` 强调 large-rank recovery
    - `ja_en` 保留失败/边界案例，避免过度包装
  - 对应文件：
    - `reports/transfer/transfer_case_analysis_examples.md`
    - `reports/transfer/transfer_case_analysis_examples.csv`
- 第四章效率分析小节：
  - 可直接使用当前 wall-clock 汇总表，但需明确“当前只完成时间统计，显存仍待补测”。
  - 对应文件：
    - `reports/transfer/transfer_efficiency_summary.md`
    - `reports/transfer/transfer_efficiency_summary.csv`
- 第五章结论与展望：
  - 吸收本轮结论时，建议写成：
    - 当前 `4` 个目标域 `5-seed` 正增益已有统计支持；
    - 案例分析进一步提供定性证据；
    - 效率部分目前先报告 wall-clock，完整时间-显存联合评估仍待补充。
- 后续若线程A继续补强：
  - `H3` 缺失率压力测试优先回填到第四章补充实验/鲁棒性分析小节；
  - GPU 显存优先回填到第四章效率分析小节；
  - 若额外 baseline 来不及补，建议回填到第五章局限性与展望。

### 4.6 当前仍不宜夸大的部分

- 不宜夸大 `source_select` 与 `missing_gate` 的单独有效性。
  - 当前更稳的表述仍应聚焦“目标域自适应 + 伪标签质量控制”。
- 不宜把当前案例分析写成大规模统计规律。
  - 当前 `6` 个案例更适合作为定性证据。
- 不宜把 wall-clock 效率分析写成完整效率结论。
  - 当前尚未统一补齐 GPU 峰值显存。
- 不宜写成“当前已经完成跨骨干泛化验证”。
  - 目前公平迁移对比仍主要限制在 `MEAformer-based transfer setting`。

### 4.7 可直接粘贴给论文线程的总结段落

- 当前最稳的主结论没有变化，仍然应围绕“统一迁移实验链路已经成立，4 个目标域的 5-seed 正式主表相对 baseline 均为正增益”来写。本轮新增的最有价值材料不是新的主实验，而是三类证据补强：第一，显著性分析已经补齐，4 个目标域在 `avg MRR` 上都是 `5/5 seed` 正增益，paired bootstrap `95% CI` 下界均大于 `0`，one-sided sign test / Wilcoxon 均为 `p=0.03125`；第二，案例分析已经补出 `6` 个代表性样本，覆盖 `ja_en` 的失败边界案例以及 `FBDB15K/FBYG15K` 的成功纠错案例；第三，效率分析已经补出 wall-clock 汇总表。当前仍不宜夸大的部分是：`source_select` 和 `missing_gate` 的单独正效应仍不够硬，`H3` 缺失模态压力测试尚未完成，额外 baseline 也尚未补入。因此论文线程下一步最值得优先吸收的是第四章中的“统计显著性与稳定性分析”“案例分析”“效率分析”三个小节；若还有时间，再等待线程A补 `H3` 最小版缺失率压力测试和 GPU 峰值显存测量。

### 4.8 增量更新（2026-03-14，H3/GPU 基础能力已接入，结果尚未产出）

- 本轮新增完成内容：
  1. 已在 `MEAformer` 中接入“人工图像缺失率注入”能力，用于后续最小版 `H3` 缺失模态压力测试。
  2. 已在 `MEAformer` 主训练入口中接入 GPU 峰值显存日志输出，后续 run 可直接记录 `[gpu_peak] allocated/reserved`。
  3. 已新增最小版 `H3` 运行脚本与汇总脚本，形成固定入口和固定产物路径。
  4. 已完成编译与 dry-run 验证，说明命令链路和参数透传均正常。
- 本轮尚未完成：
  1. 还没有真正跑出 `H3` 的正式实验结果。
  2. 还没有跑出包含 GPU 峰值显存的正式补测结果。
- 本轮新增/更新文件：
  - `baselines/MEAformer/config.py`
    - 新增：`img_mask_drop_rate`, `img_mask_drop_seed`
    - 作用：为缺失模态压力测试提供参数入口
  - `baselines/MEAformer/src/data.py`
    - 新增：按给定比例从现有图像中人为移除一部分图像特征，并同步更新 `img_mask`
    - 作用：模拟目标域图像缺失
  - `baselines/MEAformer/main.py`
    - 新增：训练结束后记录 `[gpu_peak] allocated_mb / reserved_mb`
    - 作用：为最小版显存补测提供日志证据
  - `scripts/run_meaformer.py`
    - 新增：`img_mask_drop_rate`, `img_mask_drop_seed` 参数透传
  - `scripts/run_h3_missing_modality_minimal.py`
    - 作用：运行 `baseline / v1_full / wo_missing_gate × drop_rate × seed` 的最小版 H3 实验矩阵
  - `scripts/summarize_h3_missing_modality.py`
    - 作用：汇总 `avg Hits@1 / avg Hits@10 / avg MRR / GPU peak`
  - `reports/robustness/h3_missing_modality_minimal_summary.md`
  - `reports/robustness/h3_missing_modality_minimal_summary.csv`
  - `reports/robustness/h3_missing_modality_minimal_per_run.csv`
    - 说明：当前已生成空表骨架，但还没有正式结果
- 对论文线程的回填建议：
  - 这批新增内容暂时还不能直接回填正文结论，但可以视为“待补实验基础设施已完成”。
  - 一旦后续跑出结果，优先回填到：
    - 第四章补充实验/鲁棒性分析小节（H3）
    - 第四章效率分析小节（GPU 峰值显存）
- 当前仍不宜夸大：
  - 现在只能说“`H3` 与 GPU 补测的基础能力已经接入并验证命令链路可用”，不能说“相关实验已经完成”。
- 可直接粘贴给论文线程的简短说明：
  - 线程A已完成 `H3` 缺失模态压力测试与 GPU 峰值显存补测所需的基础能力接入，包括图像缺失率注入、GPU 峰值显存日志、最小版运行脚本与汇总脚本。目前这些能力已通过编译和 dry-run 验证，但正式实验结果尚未产出，因此论文线程暂不需要改写结论，只需知道后续这两项补证已经具备可执行入口。

## 5. 线程B收到回填后的处理记录（由论文线程追加）

### 5.1 更新时间

- `2026-03-14`

### 5.2 已吸收进论文的材料

1. 统计显著性与稳定性分析。
2. 6 个案例分析。
3. wall-clock 效率分析。
4. 相应的讨论与局限性表述。

### 5.3 当前仍等待回填的材料

1. `H3` 缺失模态压力测试。
2. GPU 峰值显存测量。
3. 条件允许时的额外 baseline 或更强局限性表述。
4. 可能继续扩展的案例与图表友好数据。

### 5.4 论文线程下一步写作计划

1. 继续统一正文引文编号、参考文献格式和终稿化排版细节。
2. 在不改变主结论边界的前提下，持续吸收线程A后续补出的 `H3/GPU` 结果并回填第四章与第五章。
3. 若线程A迟迟无法产出正式结果，则把对应内容进一步收束为更明确的局限性与风险说明。
4. 继续把当前工作稿整理成更接近提交版的论文文档。

### 5.5 线程B本轮处理结果（2026-03-14 晚）

1. 已确认并吸收线程A新增的 `4.8` 交接内容，即：
   - `H3` 缺失模态压力测试与 GPU 峰值显存补测的基础能力已经接入；
   - 但正式结果尚未产出。
2. 基于上述状态，论文线程本轮**没有**把 `H3/GPU` 写成已完成实验结果，而是继续在第四章和第五章中保持“待补正式结果、当前仅能保守表述”的写法。
3. 论文线程本轮新增完成：
   - 参考文献初版
   - 致谢草稿
   - 附录骨架
   - 对论文末尾结构的补全
4. 论文当前已经基本稳定的部分包括：
   - 中英文摘要
   - 第一章至第五章主体内容
   - 第四章中的主实验、显著性、案例、wall-clock 效率和局限性分析
   - 图 1-1、图 3-1、图 4-1 至图 4-4

### 5.6 线程B当前仍等待的关键结果

1. 最小版 `H3` 正式压力测试结果
2. 最小版 GPU 峰值显存正式补测结果
3. 若上述两项暂时无法完成，则至少需要论文可直接使用的阻塞说明与局限性段落
4. 条件允许时扩展到 `8` 个案例或补 1 个额外 baseline 的最终判断
